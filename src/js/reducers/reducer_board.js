import { randomBoard } from '../model/model.js'

const defaultState = {
  board: randomBoard()
}

export default (state = defaultState, action) => {
  switch (action.type) {
    case expression:

    default:
      return state
  }
}
